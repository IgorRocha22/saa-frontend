{
  "builds": [
    {
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "pnpm install",
        "buildCommand": "pnpm build",
        "outputDirectory": "dist"
      }
    }
  ]
}
